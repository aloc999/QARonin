[project]
name = "selfheal"
version = "0.1.0"
description = "Agentic self-healing locator engine with offline heuristic mode"
requires-python = ">=3.10"
dependencies = ["requests>=2.31"]

[tool.pytest.ini_options]
testpaths = ["tests"]
