# Selenium-Python Framework (Phase 2)

Placeholder for the Selenium WebDriver framework built on the same demo
target. Planned scope:

- Selenium 4 WebDriver manager with local Chrome/Firefox drivers and optional
  Selenium Grid execution
- Same Page Object Model contract as `frameworks/playwright-ts` to allow
  engine-level comparison
- Cross-browser matrix runs wired into the L3 tier of the CI workflow
- Explicit vs implicit wait policy documented in TEST-STRATEGY.md updates
