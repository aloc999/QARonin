{
  "type": "object",
  "required": ["count", "orders"],
  "properties": {
    "count": {"type": "integer", "minimum": 0},
    "orders": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "username", "status", "total"],
        "properties": {
          "id": {"type": "integer"},
          "username": {"type": "string"},
          "status": {"type": "string"},
          "total": {"type": "number", "minimum": 0}
        }
      }
    }
  }
}
